name: upload a malicious artifact

on: [pull_request]

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "legit artifact" >> malicious
      - uses: actions/upload-artifact@v2
        with:
          name: bin
          path: malicious
